import * as React from 'react';
import FilterButtonStyles from './GranitFilterButton.Styles';

const FilterButton = () => {
    return (
        <FilterButtonStyles variant="contained" disableElevation>
        Применить фильтр
        </FilterButtonStyles>
    )
}

export default FilterButton;
